/**
 * ========================================================================
 * MODELS INDEX - Export all models
 * ========================================================================
 */

const Listing = require("./Listing");
const Offer = require("./Offer");

module.exports = {
  Listing,
  Offer,
};
